some_variable: f32
other_variable: [f32] = [2,3,4,1]

in:(float, float) out:(uint, uint, int), side:(some) {
    let x = in.0;
    x += 1;
    if x > in.0 {
        return (5, 0, 0);
    }

    return (x, 0, 0);
}

fn help_method() -> float{

}

